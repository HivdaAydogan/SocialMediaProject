server:
  port: 8889

spring:
  application:
    name: config-server-git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/HivdaAydogan/Java8ConfigGitServer.git
          default-label: main
          skip-ssl-validation: true
          username: HivdaAydogan
          password: ${java8configpassword}